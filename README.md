# PsychologHan

This workspace keeps extracted game data in `resources/` and generated deliverables in `artifacts/`; neither directory is committed.
Keep reviewable localization sources in `localization/`.

